#pragma once

#include "utils.h"

//window signals
#define FULLSCREEN   (1 << 0)
#define VISIBLE      (1 << 1)
#define PAUSED       (1 << 2)
#define CLOSABLE     (1 << 3)
#define RESIZABLE    (1 << 4)
#define CLOSED       (1 << 5)
