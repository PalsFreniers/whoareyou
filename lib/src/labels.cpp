#include "../headers/labels.h"
#include <SFML/System/Vector2.hpp>

label::label(sf::Vector2f position, sf::Vector2f size, std::string name, sf::Vector2f textOffset, int point, bool hasBackground, bool isAnimated, int animationSpeed)
    : Gameobject(position, size, name), 
{
}
